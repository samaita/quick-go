package generated

// Code generated by quickgen. DO NOT EDIT.
// Re-run: quickgen --schema <your-schema.sql> --force

import (
	"database/sql"

	"github.com/go-chi/chi/v5"
	"github.com/samaita/quick-go/generated/handler"
	"github.com/samaita/quick-go/generated/repo"
)

// RegisterRoutes wires all generated CRUD routes onto the given router.
// Call this from your server's main.go after applying auth middleware.
func RegisterRoutes(r chi.Router, db *sql.DB) {
{{- range .}}
	register{{.GoName}}Routes(r, db)
{{- end}}
}

{{range .}}
func register{{.GoName}}Routes(r chi.Router, db *sql.DB) {
	h := handler.New{{.GoName}}Handler(repo.New{{.GoName}}Repo(db))
	r.Route("/api/{{.URLPath}}", func(r chi.Router) {
		r.Get("/", h.List)
		r.Post("/", h.Create)
		r.Get("/{id}", h.GetByID)
		r.Put("/{id}", h.Update)
		r.Delete("/{id}", h.Delete)
	})
}
{{end}}
